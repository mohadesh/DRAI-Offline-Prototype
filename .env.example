# DRAI Offline — Local development environment
# Copy this file to .env and adjust values as needed.
# The .env file is not committed to git.
#
# For Docker/production deployment, see docker/.env.example
# ─────────────────────────────────────────────────────────────

# ── Simulated prediction mode ────────────────────────────────────────────────
# Set to 1 (or true/yes/on) to bypass ML models and use demo mode:
# predicted values are derived from actual MDNC_M_D / MDNC_C tags
# with ±5–15 % random noise. Useful for demos and environments without models.
#
# 0 = use real ML models (default)
# 1 = simulated predictions
DRAI_SIMULATE_PREDICTIONS=0
